const config = {
  baseUrl: import.meta.env.VITE_BASE_URL,
  googleKey: import.meta.env.VITE_GOOGLE_KEY,
  apiKey: import.meta.env.VITE_API_KEY,
  envKey: import.meta.env.VITE_ENV,
};

export default config;
